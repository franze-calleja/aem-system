export default function InterventionFeedbackPage() {
  return (
    <div className="rounded-2xl border border-dashed border-slate-300 bg-white p-8">
      <h1 className="text-lg font-semibold text-slate-900">Intervention Feedback</h1>
      <p className="mt-2 text-sm text-slate-600">
        Active intervention plans (public fields), session logging, observation notes, and revision requests
        arrive in Phase 3 (Intervention Module). Counseling notes and sensitive plan fields will remain hidden.
      </p>
    </div>
  );
}
