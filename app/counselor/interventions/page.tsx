export default function CounselorInterventionsPage() {
  return (
    <div className="rounded-2xl border border-dashed border-slate-300 bg-white p-8">
      <h1 className="text-lg font-semibold text-slate-900">Intervention Builder</h1>
      <p className="mt-2 text-sm text-slate-600">
        The multi-scope intervention module (individual, section, grade-level, school-wide) lands in Phase 3.
        It includes the AI-assisted recommendation queue, public/sensitive plan fields, principal approval workflow,
        and the revision/feedback loop with teachers.
      </p>
      <p className="mt-2 text-xs text-slate-500">
        Until then, this page is intentionally empty — Phase 2 focused on getting real data into the system.
      </p>
    </div>
  );
}
